fips_begin_module(Font)
    fips_vs_warning_level(3)
    fips_vs_disable_warnings(4267 4996)
    fips_include_directories(.)
    fips_src(. GROUP_FOLDERS)
    fips_deps(AzCore Renderer)
fips_end_module()
if (FIPS_CLANG)
    set_target_properties(Font PROPERTIES COMPILE_FLAGS "-Wno-unused-function")
endif()
