fips_begin_lib(bx)
    if (CMAKE_BUILD_TYPE MATCHES "Debug")
        add_definitions("-DBX_CONFIG_DEBUG=1")
    endif()
    fips_include_directories(3rdparty)
    fips_src(include GROUP_FOLDERS)
    fips_src(src GROUP_FOLDERS)
    if (FIPS_LINUX)
        fips_deps(dl)
    endif()
fips_end_lib()